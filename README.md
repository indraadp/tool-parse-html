# Parse HTML

Tool Parse HTML ini berfungsi untuk menyesuaikan kode HTML agar dapat ditampilkan dengan benar di CMS Blogger. Biasanya metode Parse HTML ini digunakan saat menyisipkan kode HTML di dalam artikel atau memasang kode script iklan Google Adsense di dalam template agar tidak error saat disimpan.